## Environment Variables Template for Iris Classification Project
# =============================================================
# Copy this file to .env and fill in your specific values
# Note: .env is included in .gitignore for security

# Project Configuration
PROJECT_NAME=iris-classification-tutorial
PROJECT_VERSION=1.0.0
ENVIRONMENT=development  # Options: development, staging, production

# Data Configuration
DATA_SOURCE=sklearn  # Options: sklearn, csv, url, database
DATA_URL=https://archive.ics.uci.edu/ml/machine-learning-databases/iris/iris.data
DATABASE_URL=postgresql://username:password@localhost:5432/iris_db

# Model Configuration
MODEL_SAVE_PATH=models/
RANDOM_STATE=42
TEST_SIZE=0.2

# Logging Configuration
LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FILE=logs/iris_classification.log
ENABLE_FILE_LOGGING=true
ENABLE_CONSOLE_LOGGING=true

# API Configuration (if deploying as API)
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=4
API_RELOAD=false

# MLflow Configuration (if using MLflow)
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_EXPERIMENT_NAME=iris-classification
MLFLOW_RUN_NAME=default

# Weights & Biases Configuration (if using W&B)
WANDB_PROJECT=iris-classification
WANDB_ENTITY=your-wandb-username
WANDB_API_KEY=your-wandb-api-key

# Cloud Storage Configuration (if using cloud storage)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_DEFAULT_REGION=us-west-2
S3_BUCKET=your-s3-bucket-name

AZURE_STORAGE_ACCOUNT=your-azure-account
AZURE_STORAGE_KEY=your-azure-key
AZURE_CONTAINER_NAME=your-container

GCP_PROJECT_ID=your-gcp-project
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json
GCS_BUCKET=your-gcs-bucket

# Database Configuration (if using external database)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=iris_db
DB_USERNAME=your-username
DB_PASSWORD=your-password
DB_SSL_MODE=require

# Redis Configuration (if using Redis for caching)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=your-redis-password

# Email Configuration (for notifications)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=your-email@gmail.com
EMAIL_TO=recipient@example.com

# Slack Configuration (for notifications)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK
SLACK_CHANNEL=#machine-learning

# Docker Configuration
DOCKER_IMAGE_NAME=iris-classifier
DOCKER_TAG=latest
DOCKER_REGISTRY=your-registry.com

# Jupyter Configuration
JUPYTER_PORT=8888
JUPYTER_TOKEN=your-secure-token

# Performance Configuration
MAX_WORKERS=4
MEMORY_LIMIT=4GB
TIMEOUT=300

# Security Configuration
SECRET_KEY=your-secret-key-here
ENCRYPT_MODELS=false
MODEL_ENCRYPTION_KEY=your-encryption-key

# Monitoring Configuration
ENABLE_METRICS=true
METRICS_PORT=9090
HEALTH_CHECK_INTERVAL=30

# Feature Flags
ENABLE_HYPERPARAMETER_TUNING=true
ENABLE_CROSS_VALIDATION=true
ENABLE_FEATURE_IMPORTANCE=true
ENABLE_MODEL_PERSISTENCE=true
ENABLE_VISUALIZATIONS=true

# Development Configuration
DEBUG=false
VERBOSE_LOGGING=false
PROFILE_PERFORMANCE=false
SAVE_INTERMEDIATE_RESULTS=false

# Testing Configuration
RUN_INTEGRATION_TESTS=true
RUN_PERFORMANCE_TESTS=false
TEST_DATA_PATH=tests/data/

# Deployment Configuration
DEPLOYMENT_TARGET=local  # Options: local, docker, kubernetes, heroku, aws, gcp, azure
DEPLOYMENT_URL=http://localhost:8000
HEALTH_CHECK_URL=http://localhost:8000/health

# Custom Configuration (add your own variables here)
CUSTOM_PARAMETER_1=value1
CUSTOM_PARAMETER_2=value2
